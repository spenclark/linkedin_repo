import React from "react"
import {HeaderContainer, HeaderContent} from "./HomeStyledComponents"

const Header = props => {
    return  (
        <HeaderContainer>
            <HeaderContent>

            </HeaderContent>
        </HeaderContainer>
    )
}

export default Header